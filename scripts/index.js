function preload(){
  sc1 = new spriteLoader("assets/Scene1/Scene1.json");
}
function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);
  noStroke();
  fill(255);
  textSize(32);
  textAlign(CENTER);
  text("Click to start", width/2, height/2);
}

function draw(){
    
}